<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SSnake.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SSnake"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollView x:Name="AboutPage_ScrollView" Height="150" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" ZoomMode="Disabled" ViewChanged="AboutPage_ScrollView_ViewChanged">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,5,0,40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0">
                        A basic snake game with two game modes.
                        <LineBreak/><LineBreak/>DO NOT switch modes while game is paused.
                    </TextBlock>
                    
                    <TextBlock Grid.Row="1" FontSize="16" Margin="0,15,0,0"><Bold> How to play? </Bold></TextBlock>
                    <Grid Grid.Row="2" Margin="0,10,0,0" ColumnSpacing="5" HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0"> Arrow keys </TextBlock>
                            <TextBlock Grid.Row="1"> A-W-S-D </TextBlock>
                            <TextBlock Grid.Row="2"> P </TextBlock>
                            <TextBlock Grid.Row="3"> Space Bar </TextBlock>
                        </Grid>

                        <AppBarSeparator Grid.Column="1" Width="20"/>

                        <Grid Grid.Column="2" RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0"> Changing direction </TextBlock>
                            <TextBlock Grid.Row="1"> Changing direction </TextBlock>
                            <TextBlock Grid.Row="2"> Pause/Resume game </TextBlock>
                            <TextBlock Grid.Row="3"> Restart game </TextBlock>
                        </Grid>
                    </Grid>
                </Grid>

                <Grid Grid.Row="1" ColumnSpacing="5" Margin="0,0,0,5" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Made with" FontSize="10"/>
                    <FontIcon Glyph="&#x2665;" Grid.Column="1" FontFamily="Segoe UI Emoji" FontSize="10" />
                    <TextBlock Grid.Column="2" Text="by Silicon Dioxide" FontSize="10" />
                </Grid>
                
                <Grid Grid.Row="2" ColumnSpacing="5" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#x1f517;" Grid.Column="0" FontFamily="Segoe UI Emoji" FontSize="10" />
                    <TextBlock Grid.Column="1" Text="Github:" VerticalAlignment="Center" FontSize="10" />
                    <HyperlinkButton Grid.Column="2" Content="Silincone" NavigateUri="https://github.com/silincone" VerticalAlignment="Center" CornerRadius="10" FontSize="10" />
                </Grid>
            </Grid>
        </ScrollView>

        <Button x:Name="ScrollToEndButton"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,2,2"
                CornerRadius="10" Width="34"
                Click="ScrollToEndButton_Click" Background="Transparent" BorderBrush="#10FFFFFF">
            <FontIcon Glyph="&#xE74B;" FontSize="10"/>
        </Button>
    </Grid>
</Page>
