<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SSnake.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SSnake"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SSnake">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    
    <Grid x:Name="MainWindow_Grid" Background="Transparent" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid x:Name="AppTitleBar" Grid.Column="0" Grid.Row="0" Loaded="AppTitleBar_Loaded">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/> <!-- for draggable region -->
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Image Grid.Column="0" Grid.Row="0" Margin="10,5,0,0" Source="/Assets/e_SSnake.png" Height="20"/>
            <TextBlock Grid.Column="1" Grid.Row="0" Margin="10,5,0,0" Text="SSnake" VerticalAlignment="Center"/>

            <Button x:Name="SettingsButton" Grid.Column="2" Grid.Row="0" Margin="10,5,0,0" Background="Transparent" BorderBrush="Transparent" Click="SettingsButton_Click" ToolTipService.ToolTip="Settings">
                <Button.Content>
                    <FontIcon Glyph="&#xE713;" FontSize="14" />
                </Button.Content>
            </Button>
            <Button x:Name="HeartButton" Grid.Column="3" Grid.Row="0" Margin="5,5,0,0" Background="Transparent" BorderBrush="Transparent" Click="HeartButton_Click">
                <Button.Content>
                    <FontIcon Glyph="&#xEB51;" FontSize="14"/>
                </Button.Content>
            </Button>
        </Grid>

        <local:PlayGround x:Name="PlayGround_Page" Grid.Column="0" Grid.Row="1" IsTabStop="True" TabFocusNavigation="Cycle" Margin="10,0,10,10" />
    </Grid>

</Window>
