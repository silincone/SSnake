<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SSnake.GameModes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SSnake"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="SelectMode_Key" x:DataType="local:GameMode">
            <ItemContainer AutomationProperties.Name="{x:Bind ModeName}">
                <Grid>
                    <Image Source="{x:Bind ImagePath}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="100" MaxWidth="100"/>
                    <Grid Background="#11FFFFFF" Height="Auto" VerticalAlignment="Bottom">
                        <TextBlock Padding="5,5,5,5" Text="{x:Bind ModeName}"/>
                    </Grid>
                </Grid>
            </ItemContainer>
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="GameModes_MainGrid" >
        <ItemsView x:Name="GameModes_ItemsView" Width="200" ItemTemplate="{StaticResource SelectMode_Key}" Loaded="GameModes_ItemsView_Loaded">
            <ItemsView.Layout>
                <LinedFlowLayout ItemsStretch="Fill" LineHeight="100" MinItemSpacing="5"/>
            </ItemsView.Layout>
        </ItemsView>
    </Grid>
</Page>
